name: Sync Google Sheets to Store

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Fetch and Process Google Sheet
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
          SHEET_ID: "1Vmb38osVEYcW5XN3xMyZlCJUvKNUiGqM2oHqkHB4cv0"
        run: |
          echo "Fetching data from Google Sheets ID: $SHEET_ID"
          
          # الكود هنا سيقوم بتحويل بيانات جدول جوجل إلى منتجات المتجر تلقائياً
